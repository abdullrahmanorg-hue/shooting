# Required Backend Endpoints for Notifications System

Add these endpoints to your server.js file:

1. Mark notification as read (PUT)
2. Delete notification (DELETE)

These endpoints are needed for the NotificationsCenter component to work properly.
Add them after the existing notification GET route.
